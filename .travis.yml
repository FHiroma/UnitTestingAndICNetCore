language: csharp
mono: none
dotnet: 3.0.100
solution: TPInvestigacion.sln
install:
  - sudo apt-get install -qq dotnet-sdk-2.0.0
  - bash ./tools/travis-install-dependencies.sh
script:
 - cd UnitTestMSTest.Test
 - dotnet restore
 - dotnet build
 - dotnet test
